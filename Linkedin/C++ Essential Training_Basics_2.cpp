//#include "stdafx.h"
#include <iostream>
using namespace std;
//structures
/*
struct Emp
{
    int id;
    const char *name; //char name[10] can be used but size has to be declared
    const char *position;
};

int main()
{
    Emp A = {12, "Joe", "Boss"};
    Emp *P = &A; //points to A object

    cout<<A.id<<" "<<A.name<<" "<<A.position<<endl;
    cout<<P->id<<" "<<P->name<<" "<<P->position<<endl;
    //same
    return 0;
}
*/
/*
//typedef: used to create alias of type name
typedef int i_t;

int main()
{
    i_t a=9;
    cout<<a<<endl;
    return 0;
}
*/

//auto data type used when data type to be assigned during compilation

//'=' called assignment operator
//++x, x++, --x, x--
//pre-increment, post-increment, pre-decrements, post decrements
//all increase/decrease by 1

//relational: ==, >, <, <=, >=
//logical: and(&&), or(||), not(!)

